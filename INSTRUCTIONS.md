# How to Use DragonRSense / Cómo Usar DragonRSense

> **Quick Guide to get the most out of your new assistant.**
>
> *Guía rápida para sacar el máximo provecho de tu nuevo asistente.*

---

## 🚀 Features in Action / Funcionalidades en Acción

### 1. Hover Intelligence (Inteligencia al Pasar el Mouse)

**What to do / Qué hacer:**
Move your mouse cursor over `x`, `y`, `w`, `h` properties or sprite paths like `"sprites/player.png"`.

*Mueve el cursor del mouse sobre propiedades `x`, `y`, `w`, `h` o rutas de sprites como `"sprites/player.png"`.*

**Use Case / Caso de Uso:**
- **Forgot which is width?**: Hover over `w` to confirm.
- **Checking an image**: Hover over the path to verify it's the correct sprite without opening the file.
- **¿Olvidaste cuál es el ancho?**: Pasa el mouse sobre `w` para confirmar.
- **Verificando una imagen**: Pasa el mouse sobre la ruta para verificar que es el sprite correcto sin abrir el archivo.

### 2. Color Visualization (Visualización de Colores)

**What to do / Qué hacer:**
Just write color code! `0xFF0000`, `{r: 255, g: 0, b: 0}`.

*¡Solo escribe código de color! `0xFF0000`, `{r: 255, g: 0, b: 0}`.*

**Use Case / Caso de Uso:**
- Instantly see what color a hex code represents.
- *Visualiza al instante qué color representa un código hexadecimal.*

### 3. Autocomplete (Autocompletado)

**What to do / Qué hacer:**
Start typing `"sprites/` or `require "`.

*Empieza a escribir `"sprites/` o `require "`.*

**Use Case / Caso de Uso:**
- Quickly find headers, sounds, and images without remembering exact filenames.
- *Encuentra rápidamente cabeceras, sonidos e imágenes sin recordar los nombres exactos de los archivos.*

---

## ⚙️ Configuration / Configuración

You can turn features ON/OFF in VS Code Settings (`Ctrl+,`). Search for **"DragonRSense"**.

*Puedes ACTIVAR/DESACTIVAR funciones en la Configuración de VS Code (`Ctrl+,`). Busca **"DragonRSense"**.*

| Setting / Ajuste | Description / Descripción | Recommended / Recomendado |
| :--- | :--- | :--- |
| `Coordinates > Enabled` | Explains x,y properties (Explicaciones x,y) | ✅ ON |
| `Coordinates > Show Sizes` | Explains w,h properties (Explicaciones w,h) | ✅ ON |
| `Sprites > Hover Preview` | Show image on hover (Ver imagen al pasar mouse) | ✅ ON |
| `Resources > Discovery` | Autocomplete files (Autocompletar archivos) | ✅ ON |

> **Note**: Snippets are currently always active. We plan to add a toggle for them in future updates!
>
> ***Nota**: Los Snippets están actualmente siempre activos. ¡Planeamos añadir una opción para desactivarlos en futuras actualizaciones!*
