{
  "name": "dragonrsense",
  "displayName": "DragonRSense",
  "description": "Where code stops being numbers and becomes meaning.",
  "version": "0.0.1",
  "publisher": "MagyGames",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/Macgyber/DragonRSense.git"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "dragonruby",
    "game development",
    "ruby"
  ],
  "activationEvents": [
    "onLanguage:ruby"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "dragonrsense.hello",
        "title": "DragonRSense: Hello World"
      },
      {
        "command": "dragonrsense.debugLinks",
        "title": "DragonRSense: Debug Links"
      }
    ],
    "configuration": {
      "title": "DragonRSense",
      "properties": {
        "dragonrsense.coordinates": {
          "type": "boolean",
          "default": true,
          "description": "Show hover info for X and Y coordinates"
        },
        "dragonrsense.sizes": {
          "type": "boolean",
          "default": true,
          "description": "Show hover info for W and H (width/height)"
        },
        "dragonrsense.spritePreview": {
          "type": "boolean",
          "default": true,
          "description": "Show sprite preview on hover"
        },
        "dragonrsense.resourceDiscovery": {
          "type": "boolean",
          "default": true,
          "description": "Suggest sprites, audio files, and require paths while typing"
        }
      }
    },
    "snippets": [
      {
        "language": "ruby",
        "path": "./snippets/dragonruby.code-snippets"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./ && npm run copy-locales",
    "copy-locales": "mkdir -p out/i18n/locales && cp src/i18n/locales/*.json out/i18n/locales/",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@types/node": "^20.x",
    "@typescript-eslint/eslint-plugin": "^6.x",
    "@typescript-eslint/parser": "^6.x",
    "eslint": "^8.x",
    "typescript": "^5.x",
    "@vscode/test-electron": "^2.3.x"
  }
}